{
  "name": "vlxhbtc-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:node && npm run build:web",
    "build:node": "tsc",
    "build:web": "vite build",
    "daily": "npm run build:node && node dist-node/export_token_price_daily.js && node dist-node/fetch_btc_daily.js && node dist-node/fetch_wbtc_daily.js && node dist-node/build_nav_btc_daily.js",
    "serve": "vite preview --host"
  },
  "dependencies": {
    "ethers": "^6.11.1",
    "global-agent": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
